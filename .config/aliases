# dot file management
alias dot-config='/usr/bin/git --git-dir=$HOME/.dot-config/ --work-tree=$HOME'

# ls
alias ls='ls -h -F --group-directories-first --color=auto --hyperlink=auto'
alias ll='ls -l -h -F --group-directories-first --color=auto --hyperlink=auto'

